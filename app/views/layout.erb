<!DOCTYPE html>
<html>
  <head>
  	 <title>Chitter</title>
  	</head>

  <body>
      <% if flash.now[:notice] %>
      <div id='notice'><%= flash[:notice] %></div>
      <% end %>

  		<%if @current_user %>
  		Welcome, <%= @current_user %>
  		<%end %>
  		<%= yield %>

  </body>
</html>